<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Konferansı Düzenle</title>
</head>
<body>
    <h1>Konferansı Düzenle</h1>
    <form action="/edit-conference/<%= conferenceId %>" method="post">
        <div>
            <label for="title">Konferans Başlığı:</label>
            <input type="text" id="title" name="title" value="<%= conference.title %>" required>
        </div>
        <div>
            <label for="description">Açıklama:</label>
            <textarea id="description" name="description" required><%= conference.description %></textarea>
        </div>
        <div>
            <label for="venue">Mekan:</label>
            <input type="text" id="venue" name="venue" value="<%= conference.venue %>" required>
        </div>
        <div>
            <label for="date">Tarih:</label>
            <input type="date" id="date" name="date" value="<%= conference.date %>" required>
        </div>
        <div>
            <label for="schedule">Program:</label>
            <textarea id="schedule" name="schedule" required><%= conference.schedule %></textarea>
        </div>
        <button type="submit">Konferansı Güncelle</button>
    </form>
</body>
</html>
