class IndexModel {
    async getData() {
        // Fetch data from your source here
        // For now, let's return a placeholder object
        return { message: "Hello, world!" };
    }

    user() {


        return User;
    }
}

module.exports = IndexModel;

